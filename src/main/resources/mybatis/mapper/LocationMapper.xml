<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.odd.oddProject.mapper.OddLocationMapper">
    <insert id="insertLocation" parameterType="com.odd.oddProject.dto.LocationDto">
        INSERT INTO LOCATIONS (CITY, COUNTY, DISTRICT, DONG, X, Y, del_yn) VALUES (#{CITY},#{COUNTY},#{DISTRICT},#{DONG},#{X},#{Y},'N')
    </insert>
</mapper>